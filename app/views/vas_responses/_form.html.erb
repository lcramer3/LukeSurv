<%= form_for(@vas_response) do |f| %>
  <% if @vas_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vas_response.errors.count, "error") %> prohibited this vas_response from being saved:</h2>

      <ul>
      <% @vas_response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :value %><br>
    <%= f.number_field :value %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
