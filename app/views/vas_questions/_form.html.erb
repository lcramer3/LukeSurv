<%= form_for(@vas_question) do |f| %>
  <% if @vas_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vas_question.errors.count, "error") %> prohibited this vas_question from being saved:</h2>

      <ul>
      <% @vas_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :prompt %><br>
    <%= f.text_field :prompt %>
  </div>
  <div class="field">
    <%= f.label :order %><br>
    <%= f.number_field :order %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
